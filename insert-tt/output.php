if(substr($arr[$k], 5)==4){
    if(substr($arr[$k], 7)==p){
     $A_hour=$count;
     $A_hour=($A_hour*15)/2;

  } 
  else{
    $A_hour=$count;
     $A_hour=$A_hour*15;
  }
  }

  if(substr($arr[$k], 5)==3){
    if(substr($arr[$k], 7)==p){
     $A_hour=$count;
     $A_hour=$A_hour*15;

  } 
  else{
    $A_hour=$count;
     $A_hour=$A_hour*15;
  }
  }
  